# Verify that the two return calculations are similar for small returns
comparison = pd.DataFrame({
    'Simple Returns': returns1.stack(),
    'Log Returns': returns2.stack()
})
print(comparison.head(10))  # print first 10 rows of comparison 


-->

w1​r1​+w2​r2​+⋯+wn​rn​

np.dot([0.5, 0.5], [10, 20]) = 15

https://medium.com/pythoneers/learn-numpy-numpy-cheat-sheet-4635fe4c8a92


w = np.array([0.2, 0.3, 0.5])
print(w.shape)   # (3,)
print(w.T.shape) # (3,)

w = np.array([[0.2, 0.3, 0.5]])   # 1 row, 3 columns
print(w.shape)    # (1,3)

w_T = w.T
print(w_T.shape)  # (3,1)



👉 Square root of variance = volatility (standard deviation)


✔ This line computes the total risk of your portfolio
✔ It uses both:

Your weights

How stocks move individually (variance)

How stocks move together (covariance)

✔ It accounts for diversification

If two stocks move differently → portfolio risk goes DOWN
If they move together → portfolio risk goes UP

==>

from google.colab import files
uploaded = files.upload()